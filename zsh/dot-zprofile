# Keep path arrays duplicate-free (zsh-specific)
typeset -U path manpath infopath
# Tie INFOPATH to the infopath array (not automatic in zsh, unlike PATH/MANPATH)
typeset -aT INFOPATH infopath

[ -r ~/.profile_common ] && source ~/.profile_common
